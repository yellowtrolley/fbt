{{!-- <select class="selectpicker" data-width="fit" {{action 'setLocale' on='change'}}>
  {{#each locales as |loc|}}
    <option value="{{loc.id}}" selected={{is-equal loc.id i18n.locale}}>
      {{loc.text}}
    </option>
  {{/each}}
</select> --}}




{{#bs-dropdown  tagName="li" as |dd|}}
  {{#dd.toggle}}{{i18n.locale}} <span class="caret"></span>{{/dd.toggle}}
  {{#dd.menu as |ddm|}}
  	{{#each locales as |loc|}}
    	{{#ddm.item}}<li {{action 'setLocale' loc.id}}><a href="#"> {{loc.text}}</a></li> {{/ddm.item}}
    {{/each}}
  {{/dd.menu}}
{{/bs-dropdown}}